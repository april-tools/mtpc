#!/usr/bin/bash

echo -e 'Downloading shakespeare_char models ...'
wget https://multi-token-prediction.s3.eu-west-2.amazonaws.com/shakespeare_char.gz -O - | tar -xz -C $MTP_ROOT/outputs/models
echo -e "Saved models to $MTP_ROOT/outputs/models ..."
