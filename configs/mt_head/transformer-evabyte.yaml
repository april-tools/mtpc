name: transformer

hyperparameters:
  type: 'evabyte'
  n_embd: ${lm.n_embd}
  transformer_n_head: ${lm.n_head}
  transformer_n_layer: 2       # Set to zero if you want no transformer encoder
  expander_type: 'linear'      # The available choices are 'linear', 'mlp'
  share_vocab_proj: false
